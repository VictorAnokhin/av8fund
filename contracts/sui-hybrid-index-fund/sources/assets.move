module sui_hybrid_index_fund::assets;

/// Placeholder stablecoin and RWA marker types for the MVP skeleton.
/// Replace USDCTest with the real bridged/testnet USDC type when wiring the package.
public struct USDCTest has drop, store {}
public struct RwaAapl has drop, store {}
public struct RwaBond has drop, store {}
